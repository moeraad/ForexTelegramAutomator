# 16 — Assumptions Log

Every assumption made where source material was silent or ambiguous. What was assumed, why, and what changes if the assumption is wrong.

| # | Assumption | Why | If wrong → impact |
|---|---|---|---|
| A-01 | Target subscriber is retail trader (not institutional). | Channel is Telegram + Arabic broadcast — institutional buyers don't shop here; the existing system has no segregated-account / multi-account-manager features. | Institutional reframes price + adds KYC-AML + per-fund risk + segregated reporting. ~6 months added. |
| A-02 | Subscribers bring their own MT5 broker (Path A from §1). | The existing EA is the IP; subscribers in MENA already run MT5. | If owner wants Path B (centralized MetaApi), §4-EXECUTION-LAYER.md rewrites significantly; ~8 additional engineer-weeks for credential vault + central dispatcher + central reconciler. |
| A-03 | Forex Engineer channel is licensed or owner-operated. | Without this, redistribution is litigation risk. | If unlicensed third-party with no agreement: pivot product to "build your own channel" or partner-revenue-share before any paid launch. |
| A-04 | MENA-first launch via ADGM. | Channel language + buyer geo match; ADGM has fintech-friendly path. | EU-first → Cyprus + MiFID II → ~6mo delay + capital req. US-first → infeasible at solo-founder scale. |
| A-05 | 2 engineers full-time for 6 months. | Reasonable team size to ship; matches a typical small-team SaaS launch. | Solo founder: timeline doubles. 4 engineers: timeline halves but coordination overhead grows. |
| A-06 | Flat-tier pricing $39/$99/$249. | Simplest metering; sidesteps profit-share IA framing. | Profit-share preferred: add per-trade P&L metering, complicate billing, raise IA risk. |
| A-07 | 14-day free trial, card required upfront. | Standard SaaS practice that selects intent. | No-card trial → marketing wins; conversion drops; fraud rises. $1 verify charge → friction lower than no-card; still some drop. |
| A-08 | Single symbol XAUUSD at launch. | Existing prompt + validators are gold-specific. | Multi-symbol → re-engineer state_summary block, validators, fan-out lot-sizing math. Significant. |
| A-09 | Single channel at launch. | Existing prompt + profile JSON tuned to Forex Engineer. | 2 channels at launch → 2× profile work + 2× listener instances + admin UX changes. Defer to year-2. |
| A-10 | Conservative default risk profile for new subscribers (0.005 LotsPer100Balance). | First-loss panic is the single biggest churn driver in copy services. | Owner wants aggressive defaults: increase first-loss exposure, churn risk up; product can offer "Aggressive defaults (recommended for experienced)" toggle in onboarding step 3. |
| A-11 | Subscriber's broker `account_balance_cents` updated by EA every 60s. | Signal fan-out lot math needs fresh balance. | If EA can't deliver every 60s (e.g., broker rate-limit on `ACCOUNT_BALANCE` reads), we accept stale-by-up-to-5min and use last known with a "stale balance" badge. |
| A-12 | Lot sizing is server-side; EA receives a resolved lot in payload. | Centralizes risk math; keeps subscriber UX consistent. | If EA-side lot math survives (current behavior), subscriber risk UI must mirror EA inputs exactly — duplication. |
| A-13 | Status-guarded `post_result` is preserved verbatim (REVIEW.md P0). | Already a known-critical fix. | If new code regresses this, race conditions corrupt audit rows. **Cutover gate**: test in CI. |
| A-14 | The 2026-05-27 dedup-by-action-id fix at `ea/CopyTrades.mq5:1461` is preserved verbatim in EA v2. | Real money-impacting historical incident. | Without it: duplicate broker orders on watching→sent recycling. CI regression test required. |
| A-15 | Idempotency keys on EA POSTs are `sha256(action_id || token_hash || attempt_n)`. | Deterministic from EA-side state. | If EA can't compute a stable token_hash (token rotation mid-attempt), keys diverge. Mitigation: token rotation invalidates pending attempts. |
| A-16 | One bot per 500 active Telegram-opted subscribers; shard by `subscriber_id % bot_count`. | Telegram rate limits. | If subscribers move chat_ids (re-opt-in via different account), sharding stable since we shard by `subscriber_id`. |
| A-17 | Default notification channels: email + in-app for Starter; +Telegram for Pro; +webhook for Elite. | Matches the §7 tier table. | Owner wants Telegram on Starter: cost up (more bot scaling) but onboarding wins. |
| A-18 | KYC at threshold ($25k account size or Elite tier) not at signup. | "Publication of signals" framing keeps KYC light at signup. | Counsel rules KYC required at signup: every subscriber goes through Persona; conversion drops; vendor costs up. |
| A-19 | We do NOT auto-replay missed signals after subscriber downtime. | The price moved; replaying is a different trade. | Owner wants auto-replay: per-action-type policy with "stale-OK" subset; lots of edge cases (REINFORCE on a missed close). |
| A-20 | EA v2 closed-source binary distribution; signed `.ex5`. | Protects the staged-management policy IP. | Open-source EA: trust narrative improves; copy-risk increases; code-sign no longer matters; some subscribers may modify and complain about behavior. |
| A-21 | Subscriber-installed EA on subscriber's Windows machine OR our managed-VPS add-on. | Path A. | If "we host all EAs on shared infra for everyone": violates Path A's regulatory framing; pulls toward Path B. |
| A-22 | EA cert pinning is a single SHA256 input. | Simplest tamper resistance. | If MT5 build changes WebRequest internals: needs a CA-pinning fallback. Have an `ea_cert_pin_bypass` system flag for emergencies (per RB-07). |
| A-23 | Postgres RLS enforces subscriber-scoped reads; service-role bypasses for workers. | Defense in depth. | If RLS is misconfigured, leaks across subscribers. **Mandatory test** per table: A cannot read B's row. |
| A-24 | All timestamps stored as `timestamptz` in Postgres. | Avoids the SQLite ISO-8601-string fragility documented in `docs/05-STATE-AND-DATA.md`. | If any legacy code paths assume naive strings, they fail loudly — preferred to silent timezone drift. |
| A-25 | The signal_actions row exists once per LLM-emitted action; subscriber_actions are derived. | Two-tier model for fan-out idempotence. | If a single LLM output produces 0 signal_actions (e.g., ALERT), there's still ≥1 signal_actions row (the ALERT itself) so the admin view stays consistent. |
| A-26 | OPEN action's `expires_at = created_at + 15min`; management actions `+ 5min`; REOPEN_LAST `+ 30min`. | Trades go stale; management is more time-sensitive. | Channel-specific overrides exist via `provider_settings.expiry_overrides_json`. |
| A-27 | One signal_provider in v1 (Forex Engineer); schema supports N. | The prompt + profile are channel-specific work. | Adding a second provider = ~4 engineer-weeks (profile authoring + worked-example curation + replay validation). |
| A-28 | Operator-side cutover happens before any external paying subscriber is onboarded. | Operator is subscriber #1; cutover risk is contained to operator. | If we onboard externally during cutover, data divergence on rollback is real. **Hard rule in §12**: no external subs until 14d stable on new platform. |
| A-29 | LLM provider failover Anthropic → OpenAI. | Both are wired in current code. | If OpenAI's gpt-5 differs materially in interpreter behavior, "degraded interpreter" banner must convey that risk to subscribers. |
| A-30 | Daily `pg_dump` cross-region to `eu-central-1`. | RPO 5min + cross-region durability. | If `eu-central-1` is unavailable (rare), backups still in same-region RDS PITR. |
| A-31 | Sentry beforeSend + Datadog grok pipelines strip bearer tokens, email, subscriber name, broker creds. | PII hygiene. | Test in CI: a synthetic error containing `Bearer abc123` is stripped before sink. |
| A-32 | Cost-guard kill-switch is per-provider, not per-subscriber. | Provider's daily budget caps total LLM spend regardless of fan-out N. | Per-subscriber LLM budget would require: subscribers consume LLM only via "replay" feature; otherwise LLM cost is provider-side. |
| A-33 | Webhook secrets are 1-per-URL, rotatable, HMAC SHA256. | Standard practice. | If owner prefers per-subscriber secret: change `notifications.webhook_secret_id` shape. |
| A-34 | We charge in USD; FX conversion done by Paddle for non-USD locales. | Simplest. | Local-currency pricing reduces friction but adds revenue-recognition complexity. |
| A-35 | Geo-block at Cloudflare WAF + app-layer double-check. | Defense in depth. | Cloudflare-only is bypassable via origin direct hit; app-layer-only delays the block. Both is correct. |
| A-36 | Subscribers in amber jurisdictions get KYC at signup; green jurisdictions at threshold. | Counsel-defined; placeholder. | Counsel may require KYC for all paid subscribers; cost + friction up. |
| A-37 | Founder is sole signatory on KMS CMK rotation policy. | Reduces accidental rotation. | Bus factor 1. Mitigation: documented runbook + 30-day deletion grace; second key custodian onboarded by Phase 4. |
| A-38 | All audit tables are append-only (no UPDATE, no DELETE). | Regulator-grade record-keeping. | If a customer requests deletion: PII fields scrubbed via UPDATE (allowed by policy exception), structural rows preserved. |
| A-39 | The "stack" / multi-stack concept (one per channel + one per broker) is discarded; replaced by `(signal_provider, broker_connection)` pair. | Clean tenant model. | If the operator runs a 2-channel setup today via NSSM, migration must merge both stacks into one cloud install — extra work in migration script. |
| A-40 | Each operator (admin/support) has Clerk MFA enforced. | Reasonable security baseline for admin access. | Owner OK with this; no operator login without MFA. |
| A-41 | EA v2's `connection_config_version` push avoids EA re-attach for risk-profile edits. | Subscriber UX. | If MQL5's `GlobalVariable` round-trip can't atomically persist new config: subscriber must re-attach (minor friction; acceptable). |
| A-42 | The interpreter prompt's `_TEMPLATE` is touched only via the `admin/providers/[id]/profile` versioned publish flow. | The prompt is the moat; ad-hoc edits invite drift. | Any prompt edit must pass `test_management_replay.py` and `test_replay.py` in CI before publication. |
| A-43 | `provider_settings.auto_execute_delay_sec` default is 0 (matches current behavior). | Today the system auto-promotes immediately. | Owner wants a 30s "approve in app" window: feature is already there (delay > 0); no code change, just config. |
| A-44 | The first paying external subscriber arrives at week ~24 (Phase 5). | Soft launch with friends-and-family. | If earlier, Phase 4 (ops tooling) must compress — risk that incidents land with no runbook. |
| A-45 | Total infra+vendor monthly cost at 100 paid subs ~$1,500 (per §10 estimates, marked verify). | Margin healthy at $99 × 100 = $9,900 MRR. | If real cost is 2× estimate, margin still works; if 5×, prices must rise. Monitor cost-per-sub monthly. |
